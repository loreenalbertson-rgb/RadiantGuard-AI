# Optional Phase 2 medical-imaging research dependencies.
# These packages are intentionally separate from the live foundation build
# until the model integration and deployment tests pass.

-r requirements.txt

torch>=2.7,<3.0
torchvision>=0.22,<1.0
monai>=1.5,<2.0
torchxrayvision==1.5.2
